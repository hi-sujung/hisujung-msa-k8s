apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-deployment
  labels:
    app: msa-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: msa-redis
  template:
    metadata:
      labels:
        app: msa-redis
    spec:
      containers:
      - name: msa-redis
        image: redis:7.2.4-alpine3.19
        ports:
        - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  labels:
    app: msa-redis
spec:
  selector:
    app: msa-redis
  ports:
  - protocol: TCP
    port: 6379
    targetPort: 6379
  type: ClusterIP
